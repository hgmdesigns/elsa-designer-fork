import { ActivityPropertyDescriptor, SelectListItem } from "../models";
export declare function getSelectListItems(serverUrl: string, propertyDescriptor: ActivityPropertyDescriptor): Promise<Array<SelectListItem>>;
